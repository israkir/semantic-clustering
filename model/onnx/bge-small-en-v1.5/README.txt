BGE-small-en-v1.5 for the ONNX embedding runtime (same layout as tool-mismatch-clustering).

- vocab.txt — normal Git object
- model.onnx — Git LFS (*.onnx in repo root .gitattributes)

After clone:

  git lfs install && git lfs pull

Or from repo root:  make git-lfs-pull
